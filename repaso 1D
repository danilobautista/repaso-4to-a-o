#include <iostream>
using namespace std;

int main() {

    char letra;

    cout << "Poné una letra bro: ";
    cin >> letra;

    if (letra == 'a' || letra == 'e' || letra == 'i' || letra == 'o' || letra == 'u') {
        cout << "Es una vocal";
    }
    else {
        cout << "Es una consonante";
    }
    return 0;
}
